const express = require('express');
const server = express();
server.use(express.json());
server.listen(3000);
// o código também terá o uso do framework typescript
case () => {
    function test('deve Lidar com numeros negativos' => {
        expect(somar(-10,-40) .ToBe(1)); 
        let numero1 = -10;
        let numero2 = -40;
        let resultado = -50
        console.log(resultado)//exiba o resultado -50
    })
}